<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@color/background">

    <!-- Buscador + chip modo exploración -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <EditText
            android:id="@+id/etSearchFriend"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:background="@drawable/bg_chip_glass"
            android:hint="Buscar..."
            android:textColorHint="#55FFFFFF"
            android:textColor="@android:color/white"
            android:textSize="13sp"
            android:paddingHorizontal="12dp"
            android:inputType="text"
            android:singleLine="true" />

        <!-- Chip modo exploración -->
        <!-- TODO: cuando esté activo cambiar fondo a app_color con alpha -->
        <TextView
            android:id="@+id/chipExplore"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/bg_chip_glass"
            android:text="🚀 Explorar"
            android:textColor="#AAFFFFFF"
            android:textSize="12sp"
            android:gravity="center"
            android:paddingHorizontal="12dp" />

    </LinearLayout>

    <!-- Lista de amigos / resultados exploración -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvFriends"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:nestedScrollingEnabled="false" />

</LinearLayout>